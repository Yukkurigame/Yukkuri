
parent = "live_object"

actions = {
	{
		name = "init",
		frames = {
			{ com = constants.acSuper, text = "init" },
			{ com = constants.acPushString, text = "damage" },
			{ com = constants.acPushInt, param = constants.uChrDamage },
			{ com = constants.acPushString, text = "speed" },
			{ com = constants.acPushInt, param = constants.uChrSpeed },
			{ com = constants.acLoadParamBunch, param = 2 },
			{ com = constants.acPushInt, param = constants.uStateFed },
			{ com = constants.acSetParam, param = 100 },
			{ com = constants.acPushInt, param = constants.uParamExp },			
			{ com = constants.acSetParam, param = 200 },
			{ com = constants.acPushInt, param = constants.uStateExp },
			{ com = constants.acSetParam, param = 20 },
			{ com = constants.acPushInt, param = constants.uBaseLevel },
			{ com = constants.acSetParam, param = 1 },
			{ com = constants.acPushInt, param = constants.uBaseAge },
			{ com = constants.acSetParam, param = 0 },
		},
	}
}
