

parent = "object"

actions = {
	{
		name = "init",
		frames = {
			{ com = constants.acLoadParam, text = "hp", param = constants.uParamHP },
			{ com = constants.acPushInt, param = constants.uParamHP },
			{ com = constants.acCopyParam, param = constants.uStateHP },
		},
	}
}
